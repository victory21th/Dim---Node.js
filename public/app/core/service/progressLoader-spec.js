describe('progressLoader', function() {

  beforeEach(module('core'));

  it('should ...', inject(function(progressLoader) {

	//expect(progressLoader.doSomething()).toEqual('something');

  }));

});